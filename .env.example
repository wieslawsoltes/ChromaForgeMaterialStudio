# Load explicitly: node --env-file=.env server/index.js
HOST=127.0.0.1
PORT=8787
DATA_DIR=./data
# Set to 1 only for HTTPS production deployment.
SECURE_COOKIES=0
# Exact comma-separated trusted origins only; same-origin needs no entry.
ALLOWED_ORIGINS=
